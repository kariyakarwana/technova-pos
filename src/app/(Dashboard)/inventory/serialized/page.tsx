"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { useBranch } from "@/components/dashboard/BranchContext";
import { apiGet } from "@/lib/api/client";
type Unit = { id: string; serialNumber: string; status: string; createdAt: string; branch: { name: string }; product: { sku: string; name: string }; warranty: { status: string; expiresAt: string | null } | null };
export default function SerializedUnitsPage() { const { branchId } = useBranch(); const [units, setUnits] = useState<Unit[]>([]); useEffect(() => { if (branchId) void apiGet<{ data: Unit[] }>(`/inventory/units?branchId=${encodeURIComponent(branchId)}&pageSize=100`).then((result) => setUnits(result.data)); }, [branchId]); return <main className="space-y-6 p-6"><div><p className="text-xs font-semibold uppercase tracking-wider text-[#0E9384]">Inventory</p><h1 className="text-2xl font-bold">Serialized Units</h1><p className="text-sm text-slate-500">Trace individual products from receiving through sale and warranty.</p></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-4 py-3">Serial</th><th className="px-4 py-3">Product</th><th className="px-4 py-3">Branch</th><th className="px-4 py-3">Unit status</th><th className="px-4 py-3">Warranty</th><th className="px-4 py-3"></th></tr></thead><tbody className="divide-y">{units.map((unit) => <tr key={unit.id}><td className="px-4 py-3 font-mono font-semibold">{unit.serialNumber}</td><td className="px-4 py-3">{unit.product.name}<p className="text-xs text-slate-500">{unit.product.sku}</p></td><td className="px-4 py-3">{unit.branch.name}</td><td className="px-4 py-3">{unit.status.replaceAll("_", " ")}</td><td className="px-4 py-3">{unit.warranty?.status ?? "Not activated"}</td><td className="px-4 py-3"><Link href={`/inventory/serialized/${unit.id}`} className="font-semibold text-[#0E9384]">Details</Link></td></tr>)}</tbody></table></div></main>; }
