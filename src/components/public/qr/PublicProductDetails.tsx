import { BadgeCheck, Box, CalendarDays, ShieldCheck } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";

export type PublicProduct = { serialNumber: string; unitStatus: string; product: { sku: string; barcode?: string | null; name: string; description?: string | null; brand?: { name: string } | null; category?: { name: string } | null }; warranty?: { status: string; activatedAt?: string | null; startsAt?: string | null; endsAt?: string | null } | null };
export default function PublicProductDetails({ value, token }: { value: PublicProduct; token: string }) { return <div className="mx-auto max-w-4xl px-5 py-12"><div className="text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><BadgeCheck className="h-8 w-8" /></div><p className="mt-4 text-sm font-semibold uppercase tracking-[0.18em] text-[#0E9384]">Verified TechNova product</p><h1 className="mt-2 text-3xl font-bold text-slate-900">{value.product.name}</h1><p className="mt-2 text-slate-500">{value.product.description || "Authenticated serialized product information."}</p></div><div className="mt-8 grid gap-5 md:grid-cols-2"><Card className="border-slate-200"><CardContent className="space-y-4 p-6"><h2 className="flex items-center gap-2 font-bold"><Box className="h-5 w-5 text-[#0E9384]" />Product details</h2><Detail label="Serial number" value={value.serialNumber} /><Detail label="SKU" value={value.product.sku} /><Detail label="Brand" value={value.product.brand?.name || "—"} /><Detail label="Category" value={value.product.category?.name || "—"} /><div className="flex justify-between border-t border-slate-100 pt-3 text-sm"><span className="text-slate-500">Unit status</span><Badge variant="outline">{value.unitStatus}</Badge></div></CardContent></Card><Card className="border-slate-200"><CardContent className="space-y-4 p-6"><h2 className="flex items-center gap-2 font-bold"><ShieldCheck className="h-5 w-5 text-[#0E9384]" />Warranty</h2>{value.warranty ? <><Detail label="Status" value={value.warranty.status} /><Detail label="Starts" value={date(value.warranty.startsAt)} /><Detail label="Ends" value={date(value.warranty.endsAt)} />{value.warranty.status === "PENDING" && <a href={`/warranty/activate?token=${encodeURIComponent(token)}`} className="inline-flex w-full items-center justify-center rounded-lg bg-[#025148] px-4 py-3 text-sm font-semibold text-white">Activate warranty</a>}</> : <div className="rounded-xl bg-slate-50 p-5 text-sm text-slate-500"><CalendarDays className="mb-2 h-5 w-5" />No warranty record is available for this product.</div>}</CardContent></Card></div><p className="mt-8 text-center text-xs text-slate-400">This page intentionally excludes internal cost, stock location and database security information.</p></div>; }
function Detail({ label, value }: { label: string; value: string }) { return <div className="flex justify-between border-b border-slate-100 pb-3 text-sm"><span className="text-slate-500">{label}</span><strong className="text-right text-slate-800">{value}</strong></div>; }
function date(value?: string | null) { return value ? new Date(value).toLocaleDateString() : "—"; }
